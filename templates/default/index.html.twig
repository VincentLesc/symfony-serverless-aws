{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <header class="bg-white bg-opacity-10 shadow-lg sticky top-0">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 ">
            <h1 class="text-3xl font-bold leading-tight text-gray-200">
                Welcome
            </h1>
        </div>
    </header>
    <main class="bg-black">
        <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
            <div class="px-4 py-6 sm:px-0 flex-wrap content-center">
                <div class="grid bg-white rounded-lg h-96 bg-opacity-20 shadow-lg text-gray-200 flex flex-wrap content-center justify-items-center">
                    <p>
                    Ma première page avec Style !
                    </p>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
